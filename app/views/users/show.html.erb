<!-- <p id="notice"><%= notice %></p> -->

<p>
  <strong>First Name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last Name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Street:</strong>
  <%= @user.stname %>
</p>

<p>
  <strong>Nr.:</strong>
  <%= @user.stnumber %>
</p>

<p>
  <strong>City:</strong>
  <%= @user.city %>
</p>

<p>
  <strong>Zip:</strong>
  <%= @user.zipcode %>
</p>

<p>
  <strong>Tel:</strong>
  <%= @user.tel %>
</p>

<table id="users" class= "table table-striped">
  <thead>
    <tr>
      <th>Item</th>
      <th>Description</th>
      <th>Size</th>
      <th>Brand</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gears.each do |gear| %>
      <tr>
        <td><%= gear.item %></td>
        <td><%= gear.description %></td>
        <td><%= gear.size %></td>
        <td><%= gear.brand %></td>
        <td><%= gear.price %></td>
      </tr>
    <% end %>
  </tbody>
</table>




<%= link_to 'Add gear', new_user_gear_path(@user), class: 'btn btn-primary'  %>
<%= link_to 'Edit Profile', edit_user_path(@user), class: 'btn btn-primary'%>
<%= link_to 'Home', "/", class: 'btn btn-primary' %>
